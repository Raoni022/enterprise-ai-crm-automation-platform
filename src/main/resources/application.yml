spring:
  application:
    name: enterprise-ai-crm-automation-platform

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  qualification:
    hot-threshold: 75
    warm-threshold: 45
